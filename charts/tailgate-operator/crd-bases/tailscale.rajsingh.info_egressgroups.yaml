---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: egressgroups.tailscale.rajsingh.info
spec:
  group: tailscale.rajsingh.info
  names:
    kind: EgressGroup
    listKind: EgressGroupList
    plural: egressgroups
    shortNames:
    - eg
    singular: egressgroup
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.matchedPods
      name: Pods
      type: integer
    - jsonPath: .spec.dns.enabled
      name: DNS
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EgressGroup is a set of pods that egress onto the tailnet through
          one shared gateway.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EgressGroupSpec is the desired state of an EgressGroup.
            properties:
              acceptRoutes:
                description: |-
                  AcceptRoutes makes the gateway accept subnet-router and app-connector routes
                  advertised on the tailnet (--accept-routes). Defaults to true — a client egress
                  gateway normally wants whatever the tailnet makes reachable. Hot-reloadable.
                type: boolean
              dns:
                description: DNS makes matched member pods native tailnet DNS clients
                  (see MemberDNS).
                properties:
                  clusterDNS:
                    description: |-
                      ClusterDNS is the in-cluster resolver ClusterIP used as the secondary nameserver for
                      cluster.local. Auto-detected from the kube-dns/CoreDNS Service when empty.
                    type: string
                  enabled:
                    description: Enabled turns on resolv.conf injection for matched
                      members.
                    type: boolean
                  ndots:
                    description: Ndots for the injected resolv.conf options. Defaults
                      to 5 (standard Kubernetes).
                    format: int32
                    type: integer
                  searchDomains:
                    description: |-
                      SearchDomains overrides the injected search list. Defaults to
                      [<ns>.svc.cluster.local, svc.cluster.local, cluster.local].
                    items:
                      type: string
                    type: array
                type: object
              exitNode:
                description: |-
                  ExitNode routes all member egress (default route) through a chosen tailnet exit
                  node. Optional; hot-reloadable. Selecting one puts 0.0.0.0/0 + ::/0 on member
                  pods (in a dedicated policy table, never the pod main table).
                properties:
                  allowLANAccess:
                    description: |-
                      AllowLANAccess keeps the member's direct LAN/cluster reachability while the exit
                      node carries the default route. It is a property of the exit-node *client* (the
                      gateway), mapping to tailscaled's AllowLANWhileUsingExitNode
                      (== `tailscale up --exit-node-allow-lan-access`).
                    type: boolean
                  name:
                    description: |-
                      Name selects the exit node using the same value space as the native
                      `tailscale set --exit-node`: a tailnet IP, a MagicDNS name, a StableNodeID,
                      or "auto" / "auto:any" to let an eligible exit node be picked automatically.
                      Empty clears the exit node. (The declarative tailscaled config cannot carry
                      "auto", so for "auto" the operator resolves a concrete node and re-resolves on
                      a periodic requeue for failover; an explicit ref is passed through verbatim.)
                    type: string
                type: object
              mirrorRoutes:
                description: |-
                  MirrorRoutes steers every route the gateway can reach (peer AllowedIPs + accepted
                  subnet-router / app-connector CIDRs, minus cluster ranges) onto member pods, so egress
                  follows DNS resolution — e.g. an app-connector domain resolved via the gateway is then
                  reachable by the IP it resolved to, with no static spec.routes. Defaults to true when
                  DNS.Enabled (native-client mode wants resolution and egress to agree). Ignored when
                  ExitNode is set (the full-tunnel default route already covers everything).
                type: boolean
              routes:
                description: |-
                  Routes are the tailnet-reachable CIDRs to steer through the gateway onto member
                  pods — private subnet-router ranges and/or public app-connector ranges. CGNAT
                  (100.64.0.0/10) and the IPv6 ULA are always steered and need not be listed. A
                  non-empty Routes is the implicit "subnet" signal. We dial, not serve: the gateway
                  never ADVERTISES routes.
                items:
                  type: string
                type: array
              selector:
                description: Selector picks member pods (gateway-side selection; no
                  Multus needed).
                properties:
                  namespaceSelector:
                    description: |-
                      A label selector is a label query over a set of resources. The result of matchLabels and
                      matchExpressions are ANDed. An empty label selector matches all objects. A null
                      label selector matches no objects.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  podSelector:
                    description: |-
                      A label selector is a label query over a set of resources. The result of matchLabels and
                      matchExpressions are ANDed. An empty label selector matches all objects. A null
                      label selector matches no objects.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              tags:
                description: Tags for the gateway tailnet node. Defaults to ["tag:egress-<name>"].
                items:
                  type: string
                type: array
            required:
            - selector
            type: object
          status:
            description: EgressGroupStatus is the observed state of an EgressGroup.
            properties:
              advertisedRoutes:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dnsInjected:
                description: |-
                  DNSInjected is the number of member pods whose DNS the webhook mutated for native
                  tailnet resolution.
                format: int32
                type: integer
              gatewayHostname:
                type: string
              matchedPods:
                format: int32
                type: integer
              resolvedExitNode:
                description: |-
                  ResolvedExitNode is the concrete exit node the gateway was pinned to — an echo of a
                  static NodeID, or the node the operator auto-selected.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
